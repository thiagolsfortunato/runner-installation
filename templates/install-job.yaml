apiVersion: batch/v1
kind: Job
metadata:
  name: codefresh-runner-installation
  annotations:
    "helm.sh/hook": post-install
    "helm.sh/hook-delete-policy": hook-succeeded
spec:
  template:
    spec:
      serviceAccountName: {{ include "codefresh-runner.serviceAccountName" . }}
      containers:
      - name: codefresh
        image: {{ .Values.image.repository }}
        command: ["codefresh", "runner", "init", "--token", "$(CODEFRESH_TOKEN)", "--exec-demo-pipeline", "false", "--set-default-runtime", "true", "--name", "$(CF_AGENT_NAME)"]
        env:
          - name: CODEFRESH_TOKEN
            value: {{ .Values.env.codefreshToken }}
          - name: CF_ARG_KUBE_NAMESPACE
            value: {{ .Values.env.codefreshKubeNamespace }}
          - name: CF_ARG_KUBE_CONTEXT_NAME
            value: inClusterContext
          - name: CF_AGENT_NAME
            value: {{ .Values.env.codefreshAgentName }}
      restartPolicy: Never

  backoffLimit: 4